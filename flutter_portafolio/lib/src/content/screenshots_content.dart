import 'package:flutter/material.dart';

class ScreenshotsContents extends StatelessWidget {
  const ScreenshotsContents({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color:  Colors.red,
      height: 258,
    );
  }
}